# Project Context

- **Owner:** Stuart Hillary
- **Project:** Sage® Simulation and Modeling Libraries — a long-running discrete event simulation (DES) library originally built on early .NET Framework, now targeting .NET 8.
- **Stack:** C#, .NET 8, NUnit/xUnit, GitHub Actions, NuGet
- **Key modules:** Core (event engine), Scheduling, Graphs, Mathematics, SystemDynamics, ItemBased, Randoms, Persistence, Presentation, SmartPropertyBag, Utility
- **Goals:** Continued development, .NET 8 modernization, performance improvement, future visualization layer
- **PM:** Stuart Hillary (human — sets priorities and direction)
- **Created:** 2026-03-05

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

### 2026-07-15 — BenchmarkDotNet Baseline Established

**Branch:** `feature/dotnet10`  
**Work:** Created `Sage_Aux\SageBenchmarks\SageBenchmarks.csproj` with baseline DES benchmarks.

#### Hot-Path Observations (from code review)

**Executive (`Sage\Core\Executive.cs`):**
- Event queue: `System.Collections.SortedList` — field `_events`, comparer `ExecEventComparer`
- Scheduling: `RequestEvent()` → acquires `lock(_events)` on every call → `SortedList.Add(ExecEvent.Get(...), key)` — object pool (`ExecEvent.Get()`) but O(n) insert worst case
- Dispatch (hot path, line 595): `_events.GetKey(0)` + `_events.RemoveAt(0)` → **RemoveAt(0) shifts all remaining elements — O(n) per dequeue**
- Additional per-iteration overhead: `Monitor.Enter/Exit(_runLock)`, `lock(_events)`, causality checks, diagnostics checks, `ClockAboutToChange` check
- Result: **O(N²) total dequeue cost** for N events

**ExecutiveFastLight (`Sage\Core\ExecutiveFastLight.cs`):**
- Event queue: binary min-heap backed by `_ExecEvent[]` — field `_eventArray`
- Scheduling: no locks → `new _ExecEvent(...)` → `Enqueue()` sift-up (O(log n); O(1) for ascending-sorted input)
- Object pool: `ExecEventCache` recycles `_ExecEvent` instances — `_execEventCache.Return(currentEvent)` after dispatch
- Dispatch (`StartWocv()`, line 546): `Dequeue()` sift-down O(log n) → delegate invoke → `_execEventCache.Return()`
- Result: **O(N log N) total** — dramatically better at large N

**DetachableEvent (`Sage\Core\DetachableEvent.cs`):**
- `Begin()` creates `ManualResetEventSlim`, calls `Task.Run()`, blocks on `_beginResetEvent.Wait()`
- Each detachable event: 1× Task allocation, 3× ManualResetEventSlim allocations, thread pool dispatch
- Do NOT benchmark detachable events with `ExecutiveFastLight` — not supported (throws `NotSupportedException`)

**ExecFactory (`Sage\Core\ExecFactory.cs`):**
- `CreateExecutive()` → creates default `Executive` via reflection (`ConstructorInfo.Invoke`)
- `CreateExecutive(ExecType.FullFeatured)` → same
- `CreateExecutive(ExecType.SingleThreaded)` → `ExecutiveFastLight`
- Both handle missing app.config section gracefully (write to Debug output, continue)

#### Benchmark Structure

**Location:** `Sage_Aux\SageBenchmarks\`  
**Key file:** `Sage_Aux\SageBenchmarks\Benchmarks\EventDispatchBenchmarks.cs`

**Benchmarks defined:**
| Benchmark | Description |
|---|---|
| `Executive_SequentialEvents` (baseline) | Bulk schedule N ascending-time events, run `Executive` to completion |
| `ExecutiveFastLight_SequentialEvents` | Same with heap-based executive |
| `Executive_ChainedEvents` | Each handler schedules next event (queue depth ~1 throughout) |
| `ExecutiveFastLight_ChainedEvents` | Same with `ExecutiveFastLight` |

**Parameters:** `EventCount` ∈ {1000, 10000, 100000}  
**Attributes:** `[ShortRunJob]`, `[MemoryDiagnoser]`  
**BenchmarkDotNet version:** 0.15.8

#### What to Expect from Results

- At `N=100_000`, `Executive_SequentialEvents` should be **far slower** than FastLight due to O(N²) dequeue cost
- `ChainedEvents` benchmarks show per-event overhead with queue depth=1 — SortedList insert/remove cost is near-zero here; difference isolates lock overhead and object allocation differences
- `[MemoryDiagnoser]` should show FastLight allocates far less per operation due to `ExecEventCache` pooling



### 2026-07-15 — BenchmarkDotNet Build Fix (COMPLETE)

**Branch:** `feature/dotnet10`  
**Requested by:** Stuart Hillary

#### Root Cause

The repo-level `Directory.Build.props` redirects MSBuild output paths (`BaseIntermediateOutputPath`, `OutputPath`, `OutDir`, etc.) to the repo-wide `BuildOutput\` tree. BenchmarkDotNet generates a temp project (`BenchmarkDotNet.Autogenerated.csproj`) under the benchmark project's `bin\Release\net10.0\SageBenchmarks-ShortRun-1\` directory. When that temp project is built, MSBuild walks up and picks up the repo-root `Directory.Build.props`, redirecting obj paths to `BuildOutput\obj\Release\BenchmarkDotNet.Autogenerated\`. But NuGet restore had already written `project.assets.json` to the default SDK path → `error NETSDK1004: Assets file not found`.

#### Fix

Added `Sage_Aux\SageBenchmarks\Directory.Build.props` that:
1. Chains the parent (repo-root) props via ``.
2. Immediately resets `ArtifactsPath`, `BaseIntermediateOutputPath`, `IntermediateOutputPath`, `MSBuildProjectExtensionsPath`, `BaseOutputPath`, `OutputPath`, `OutDir`, `PublishDir` to empty so BenchmarkDotNet's temp projects use standard SDK layout.

This file is closest in the directory walk for both `SageBenchmarks.csproj` and the BDN temp project, so both use default paths.

#### Verification

All 12 benchmark cases completed — no NETSDK1004 error. Sample (N=1000):
- `Executive_SequentialEvents`: ~287 µs  
- `ExecutiveFastLight_SequentialEvents`: ~80 µs (3.6× faster — consistent with O(N²) vs O(N log N))
