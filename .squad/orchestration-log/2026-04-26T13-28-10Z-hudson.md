# Orchestration Log: Hudson (Phase 2 Wrapper Regression Tests)

**Date:** 2026-04-26T13:28:10Z  
**Agent:** Hudson  
**Mode:** background  
**Task:** Add regression coverage for Phase 2 utility-wrapper batch

## Outcome

✅ **Complete**

Added 10 regression tests across `HashtableOfLists`, `WeakHashtable`, and `WeakList` to lock down wrapper semantics during Parker's generic collection internals migration:

- Targeted wrapper suite: 14/14 tests passing ✅
- Full `Sage.Tests` suite: 281/281 tests passing ✅
- Regression coverage for all three wrapper classes
- Safe QA-triggered fixes: `WeakList.Insert()`, `Add()` index return, `HashtableOfLists` null-safe equality

## Files Changed

- `src\Sage\Utility\HashtableOfLists.cs` — null-safe equality check for duplicate suppression
- `src\Sage\Utility\WeakList.cs` — `Insert()` wrapping with `MyWeakReference`
- `tests\SageTestLib\TestHashtableOfLists.cs` — regression test expansion
- `tests\SageTestLib\TestWeakRefHashtable.cs` — regression test expansion
- `tests\SageTestLib\TestWeakList.cs` — regression test expansion
- `.squad\agents\hudson\history.md` — agent learning record
- `.squad\decisions\inbox\hudson-phase2-wrapper-tests.md` — decision record
- `.squad\decisions\inbox\parker-phase2-wrappers.md` — decision record (reference)

## Locked Semantics

1. **`HashtableOfLists` (non-generic):** De-duplicates identical values per key (legacy behavior)
2. **`HashtableOfLists<TKey, TValue>` (generic):** Keeps duplicates, sorts per-key lists if comparer provided, leaves empty keyed lists until `PruneEmptyLists()` or enumeration
3. **`WeakHashtable`:** Cleanup is lazy; dead entries disappear on indexer/`Values` access
4. **`WeakList`:** Operations are target-based (`Contains`, `IndexOf`, `Remove`, `Insert`, `CopyTo`, `Collapse`)

**QA-Safe Fixes:**
- `WeakList.Insert()` now wraps items with `MyWeakReference` (matches `Add()` behavior)
- `WeakList.Add()` returns inserted index post-migration
- `HashtableOfLists.Add()` uses null-safe equality for duplicate suppression

## Scope Boundary

Phase 2 regression coverage focused on wrapper public contracts and semantics. Tripwire for detecting unintended behavior changes in concurrent internals migration.

---
**Coordination:** Phase 2 testing complete. Ready for Phase 3 planning or additional scope.
