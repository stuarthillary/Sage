# Orchestration Log: Ripley — Phase 3 Resource Manager Scope Gate

**Date:** 2026-04-28T22:58:19Z
**Agent:** Ripley
**Phase:** p3-resource-manager
**Mode:** sync

## Manifest

- **Task:** Set scope gate for Phase 3 resource-manager opening slice
- **Outcome:** Narrowed scope to typed read-only `Resources` and `GetResourceManagers()` only
- **Deferrals:** waiter behavior, events, mutators, indexer semantics, XML payloads, construction/serializer identity, versioning

## Scope Gate Decision

The first breaking slice for `p3-resource-manager` is **typed read-only collection exposure only**:

- `IResourceManager.Resources` → `IReadOnlyList<IResource>`
- `ResourceManager.Resources` → `IReadOnlyList<IResource>`
- `SelfManagingResource.Resources` → `IReadOnlyList<IResource>`
- `MaterialResourceItem.Resources` → `IReadOnlyList<IResource>`
- `IResourceManagerCollection.GetResourceManagers()` → `IReadOnlyCollection<IResourceManager>`
- `ResourceManagerCollection.GetResourceManagers()` → `IReadOnlyCollection<IResourceManager>`

## Explicit Boundaries (do not change)

- `Reserve`, `Acquire`, `Unreserve`, `Release` behavior
- Waiter ordering and prioritized request semantics
- Event delegate shapes
- Add/Remove/Clear mutators
- Indexer semantics
- Constructor/serialization identity work
- XML payload shape
- Versioning work

## Validation Baseline

- `dotnet build .\src\Sage\Sage.csproj --no-restore` ✅
- `dotnet test .\tests\SageTestLib\Sage.Tests.csproj --no-build --filter "FullyQualifiedName~TestResources|FullyQualifiedName~TestServers"` ✅

## Authorization

- Implementation owner may proceed on this slice only
- Hudson to map fallout and lock regression coverage before broader resource-manager work
- Any attempt to fold in persistence, constructor, waiter, or event redesign is out of scope

## Files Affected

- `.squad/decisions/inbox/ripley-phase3-resource-manager-gate.md`
- `.squad/agents/ripley/history.md`
